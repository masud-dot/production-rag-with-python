# 03-production-api

See the corresponding project chapter in the book. Shared components live in
`src/lighthouse/`; this directory holds the project's wiring, configuration,
and entry point.

Run the shared test suite from the repository root: `python3 -m pytest`.
